# SuperAwesome code challange

Helper CLI to read a given file and print all anagrams.

run

```bash
yarn install
yarn start --help
```

for a list of available commands.

Example:

```bash
yarn start get-anagrams task/data/example1.txt
```
